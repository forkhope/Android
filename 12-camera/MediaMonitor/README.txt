一个简单的Android摄像头视频监控应用

发送端,会进行如下步骤:
    1.使用MediaRecorder类实时从摄像头获取视频流.
    2.对采集到的视频流 (目前是 H264 格式) 进行解码.
    3.将解码后的 H264 视频流发送到客户端.

接收端:
    1.使用 Socket 接收发送端的数据.
    2.将接收到的解码后的视频流传到 MediaCodec 类中,由该类来显示图片.
